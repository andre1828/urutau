//! All tests for Urutau Daemon
//! Run individual test modules via `zig build test-dbus` etc.

// This file is intentionally minimal - run specific test suites instead
// Use: zig build test-dbus, zig build test-storage, etc.

test "placeholder" {
    // Placeholder test to ensure build passes
    // Run specific test suites for actual tests
}
